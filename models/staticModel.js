const User = {
  username,
  email,
  password
}
const dream = {
  userId,
  title,
  description,
  date,
  emotions,
  tags,
  isPublic,
  imageUrl,
  tagName
}

const emotion = {
  userId,
  emotions,
  dreamId,
  intensity,
  date,
  details,
  colors
}
//(optional)
const comments = {
  dreamId,
  userId,
  text,
  date
}
